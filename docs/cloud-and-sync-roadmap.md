# 云端与同步状态

云端同步、共享数据库、公共网站和多用户部署已暂停，不属于 Personal Academic Radar v0.8.0 的支持范围。

当前唯一承诺的运行方式是：单个用户、单台主机、私有状态目录、本地 SQLite、回环地址网页服务，以及 Codex 宿主的本地导出/判断/导入工作流。GitHub 仅同步代码、测试和通用示例，不承载或镜像任何用户状态。

未来若重新讨论远程访问，必须作为独立产品决策重新评估身份认证、租户隔离、数据保管、密钥托管、备份恢复和威胁模型；现有本地应用不会提前加入同步占位功能。
